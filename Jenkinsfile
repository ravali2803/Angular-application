pipeline {
    agent any
    stages {
        stage('checkout') {
            echo 'checkout'

        }
        stage('build') {
            echo 'build'
        }
        stage('docker-build') {
            echo 'docker'
        }
        stage('docker-build') {
            echo 'hub'
        }    
    }
}